apiVersion: v1
kind: Service

metadata:
  name: backend-service
  namespace: employee-app

spec:
  selector:
    app: backend-deployment

  type: NodePort

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30500    

